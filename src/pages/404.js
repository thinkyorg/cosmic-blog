import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = ({ location }) => (
  <Layout location={location}>
    <h1>NOT FOUND</h1>
    <p>Sorry, try another page</p>
  </Layout>
)

export default NotFoundPage
